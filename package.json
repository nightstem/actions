{
  "name": "actions",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prettier": "prettier",
    "prettier:check": "prettier --check . --ignore-unknown",
    "prettier:write": "prettier --write . --ignore-unknown"
  },
  "devDependencies": {
    "prettier": "^3.3.2"
  }
}
